#import "../fragments/UserInviteFields.graphql"

query ($limit: Int, $offset: Int, $orderBy: [OrderByInput]) {
  usersInvite(limit: $limit, offset: $offset, orderBy: $orderBy) {
    ...UserInviteFields
  }
}
